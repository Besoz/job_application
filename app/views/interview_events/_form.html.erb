<%= form_for(@interview_event, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @interview_event.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@interview_event.errors.count, "error") %> prohibited this interview_event from being saved:</h4>

      <ul>
      <% @interview_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :score, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :score, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :application_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :application_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :interview_type_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :interview_type_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
